# include "bits/stdc++.h"
using namespace std;

// pollard rho: return any prime factor of the given number
int pollard_rho(int n)
{
    //we will pick from the range [2, N)
    int x = (rand() % (n-2)) + 2;
    int y = x;
    int c = (rand()%(n-1))+1;
    int divisor = 1;
}

int main(int argc, char const *argv[])
{
    
    return 0;
}