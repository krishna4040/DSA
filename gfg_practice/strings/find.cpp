# include "bits/stdc++.h"
using namespace std;

int strstr(string s , string x)
{
    if (s.find(x) == string::npos)
        return -1;
    return s.find(x);
}

int main(int argc, char const *argv[])
{
    
    return 0;
}